import Ember from 'ember';

export default Ember.Route.extend({
  // classNames: ['card-container', 'manual-flip', 'btn', 'hover'],
  // actions: {
  //   rotateCard(button) {
  //     this._super(...arguments);
  //     var $card = Ember.$(button).closest('.card-container');
  //
  //     if ($card.hasClass('hover')) {
  //       $card.removeClass('hover');
  //     } else {
  //       $card.addClass('hover');
  //     }
  //
  //
  //
  //   }
  // }


});
